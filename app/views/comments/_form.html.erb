<%= form_for([commentable, Comment.new], remote: true) do |form| %>
  <div class="form-group">
    <%= form.text_area :body, required: true,  placeholder: 'Add comment' %>
  </div>

  <div class="form-actions">
    <%= form.submit %>
  </div>
<% end %>
